# Garagge Environment Configuration
# Copy this file to .env and customize as needed

# Security Configuration (REQUIRED)
# Generate with: openssl rand -base64 32
JWT__SECRET=your-super-secret-jwt-key-here

# Database Security (REQUIRED) 
# Generate with: openssl rand -base64 16
DB_PASSWORD=your-secure-database-password

# PostgreSQL Container Configuration (with defaults)
DB_DATABASE_NAME=garagge-db
DB_USERNAME=postgres
DB_DATA_LOCATION=./postgres-data

# Optional: Custom Database Storage Location
# DB_DATA_LOCATION=./custom-postgres-data

# Optional: Avoid Port Conflicts - FOR DEVELOPMENT ONLY

# Optional: External Database (remove PostgreSQL container variables if used)
# DB_HOST=your-external-db.com
# DB_PORT=5432
# DB_NAME=garagge-db
# DB_USERNAME=postgres

# Optional: Development Environment
# ASPNETCORE_ENVIRONMENT=Development
# DB_HOST=db  # Override localhost when running Development in Docker
# LOG_LEVEL=Debug
